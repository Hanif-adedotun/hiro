---
// Language icons for social proof
const leftIcons = ["languages/flask.svg", "languages/go.svg", "languages/java.svg", "languages/nodejs.svg"];
const rightIcons = ["languages/python.svg", "languages/nextjs-line.svg", "languages/atom-original.svg", "languages/php-plain.svg"];
---

<section id="customers" class="relative z-10 py-20 md:py-28" aria-labelledby="customers-heading">
  <div class="container mx-auto px-4">
    <div class="mx-auto max-w-2xl text-center">
      <h2 id="customers-heading" class="opacity-0 animate-fade-in-up text-3xl font-bold tracking-tight text-foreground md:text-4xl" style="animation-delay: 0ms;">
        Seamless multi-language support
      </h2>
      <p class="mt-4 opacity-0 animate-fade-in-up text-lg text-muted-foreground" style="animation-delay: 80ms;">
        Generate unit tests and documentation for Python, Java, Go, JavaScript, and more—no matter your tech stack.
      </p>
      <div class="mt-10 flex flex-col items-center gap-4 opacity-0 animate-fade-in-up sm:flex-row sm:justify-center" style="animation-delay: 160ms;">
        <a
          href="https://hiro.hanif.one/login"
          class="inline-flex w-full items-center justify-center gap-2 rounded-lg bg-primary px-8 py-4 text-base font-semibold text-primary-foreground shadow-lg transition-all hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 sm:w-auto"
        >
          Get started free
          <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden>
            <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
      </div>
      <p class="mt-4 text-sm text-muted-foreground">Focus on building. Hiro handles the testing.</p>
    </div>

    <div class="relative mx-auto mt-16 hidden h-48 max-w-4xl md:block" aria-hidden="true">
      <div class="absolute inset-0 flex items-center justify-center gap-4">
        {leftIcons.map((src, i) => (
          <div
            class="flex h-16 w-16 items-center justify-center rounded-full border-2 border-border bg-card p-3 opacity-0 animate-scale-in shadow-sm"
            style={`animation-delay: ${200 + i * 80}ms; position: absolute; left: ${15 + i * 8}%; top: ${i % 2 === 0 ? "10%" : "60%"};`}
          >
            <img src={src} alt="" class="h-full w-full object-contain" width="32" height="32" />
          </div>
        ))}
        {rightIcons.map((src, i) => (
          <div
            class="flex h-16 w-16 items-center justify-center rounded-full border-2 border-border bg-card p-3 opacity-0 animate-scale-in shadow-sm"
            style={`animation-delay: ${220 + i * 80}ms; position: absolute; right: ${15 + i * 8}%; top: ${i % 2 === 0 ? "10%" : "60%"};`}
          >
            <img src={src} alt="" class="h-full w-full object-contain" width="32" height="32" />
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
